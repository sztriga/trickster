# Runtime
numpy==1.26.4
fastapi
uvicorn

# Training (install torch separately — see setup_aws.sh for CPU-only wheel)
# torch

# Cython extensions (build with: python setup_cython.py build_ext --inplace)
cython

# Dev / tests
pytest==8.3.4

